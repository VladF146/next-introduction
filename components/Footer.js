import styled from "styled-components";

export default function Footer() {
  return <StyledFooter>This is my header!</StyledFooter>;
}

const StyledFooter = styled.footer`
  background-color: black;
  color: white;
`;
